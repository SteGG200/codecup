#include <bits/stdc++.h>
#ifndef CONSTANT
#define CONSTANT

using namespace std;

const int inf = 1e9 + 7;
const int level = 1;

int calPoint(char s[10], vector<vector<bool>> &check, int l, int r, bool inside);

pair<string, int> max(const pair<string, int> &A, const pair<string, int> &B);


#endif // CONSTANT